services:
  nginx:
    image: nginx:latest
    container_name: nginx-jorgegarre
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/share/nginx/html/
      - ./conf/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./conf/htpasswd:/etc/nginx/.htpasswd
    restart: unless-stopped
